# Snapshots created by `pnpm eval --update`. Individual snapshot files are
# checked into git so PRs can review any prompt-output changes; this .gitkeep
# ensures the directory itself exists even before the first run.
