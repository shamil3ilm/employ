{
  "crons": [
    { "path": "/api/cron/reminders", "schedule": "0 8 * * *" },
    { "path": "/api/cron/discover", "schedule": "0 */6 * * *" }
  ],
  "buildCommand": "DATABASE_URL=$DATABASE_URL_UNPOOLED pnpm db:migrate && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile"
}
